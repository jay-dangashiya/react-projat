import { Link } from "react-router-dom";

const Products = () => {
  const productList = [
    { id: 1, name: "Laptop" },
    { id: 2, name: "Mobile" },
    { id: 3, name: "Tablet" }
  ];

  return (
    <div>
      <h1>Products Page</h1>
    </div>
  );
};

export default Products;